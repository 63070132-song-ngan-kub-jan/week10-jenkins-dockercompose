pipeline {
  agent any
  stages {
    stage('Git initialize') {
      steps {
        echo 'Pull git repository...'
        sh 'git pull origin main'
      }
    }
    stage('Docker build images') {
      steps {
        dir('week10-jenkins-dockercompose') {
          echo 'Start building Docker images...'
          sh 'docker rmi $(docker images -q) -f'
          sh 'docker-compose build'
        }
      }
    }
    stage('Docker push images to DockerHub') {
      steps {
        dir('week10-jenkins-dockercompose') {
          echo 'Pushing Docker images...'
          sh 'docker push panupongkeawkam/week10-jenkins-dockercompose-frontend'
          sh 'docker push panupongkeawkam/week10-jenkins-dockercompose-backend'
        }
      }
    }
  }
}